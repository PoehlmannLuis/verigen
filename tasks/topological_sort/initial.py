def topological_sort(graph: dict[int, list[int]]) -> list[int]:
    """Return a topological ordering of the directed graph.

    graph maps node -> list of its outgoing neighbours (edges).
    If the graph contains a cycle, raise ValueError.
    """
    # EVOLVE-BLOCK-START
    raise NotImplementedError("Replace this code!")
    # EVOLVE-BLOCK-END
